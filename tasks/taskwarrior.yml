---
# TODO; provide self-compiled 2.4+
- name: ensure taskwarrior installed
  apt: >
    name=task
    state=present
  tags:
  - shelltools

- name: ensure taskrc present
  copy: >
    src=taskrc
    dest=/home/{{ user }}/.taskrc
    owner={{ user }}
    group={{ user }}
    mode=0600
  tags:
  - shelltools
